export type SectionProps = {
    children: JSX.Element;
    styles: string,
    name: string,
  };

export type ColumProps = {
    children: JSX.Element | JSX.Element[];
    widthMobile: Number,
    widthDestok: Number,
  };
